server.port=8888
server.servlet.context-path=/lemonme





# JPA / JDBC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/lemonme?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=GMT%2B1
spring.datasource.username=root
spring.datasource.password=root
# HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#Caricamento automatico dei dati DEMO
# SPRING BATCH (BatchProperties)
# Database schema initialization mode.
#spring.batch.initialize-schema=always
# Execute all Spring Batch jobs in the context on startup.
#spring.batch.job.enabled=false
# Path to the SQL file to use to initialize the database schema.
#spring.batch.schema=classpath:InitData.sql